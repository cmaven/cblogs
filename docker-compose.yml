# ============================================================
# docker-compose.yml: cblogs 프로덕션 컨테이너 설정
# 생성일: 2026-04-13 | 수정일: 2026-04-13
# ============================================================

services:
  cblogs:
    build: .
    ports:
      - "3031:3031"
    restart: unless-stopped
