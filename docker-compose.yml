# ============================================================
# docker-compose.yml: VitePress 프로덕션 컨테이너 설정
# 생성일: 2026-04-08 | 수정일: 2026-04-08
# ============================================================

services:
  cdocs:
    build: .
    ports:
      - "3030:3030"
    restart: unless-stopped
